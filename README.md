# handwritten-arabic-similarity
A deep learning project for measuring similarity between handwritten Arabic letters using convolutional neural networks (CNN). Includes dataset preprocessing, training, and evaluation.
